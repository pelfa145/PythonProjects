import tkinter as tk
root=tk.Tk()
root.title("la lang")
root.withdraw()
def lyric(x,y):
    def spawn():
        box=tk.Toplevel(root);
        box=tk.title(":((")
        box.geometry(f"420x450+{x}+{y}")

        wlw=tk.Text(
            root,
            font=("Jetbrains Mono Nerd")
        )


        def slide():

lyric(500,500)

root.mainloop()